.sign-in-wrap(layout="column" layout-padding flex="55" layout-align="center center" ng-cloak)
  div.md-margin(layout-padding)
    div(layout-gt-sm="column")
      h4 Sign In
    form(name="login" method="post" novalidate ng-submit="loginUser()")
      div(layout-gt-sm="column" flex)
        md-input-container
          md-icon.fa.fa-users
          input(ng-model="user.username" name="username" type="username" placeholder="username" ng-required="true")
          div(ng-show="login.email.$error.required") not a valid email format

        md-input-container(flex)
          md-icon.fa.fa-key
          input(ng-model="user.password" name="password" type="password" placeholder="password" ng-required="true" ng-minlength="8")
          div(flex layout="column")
            span.error(flex ng-show="login.password.$touched && login.password.$error.minlength") Password must be not be less than 8 characters
            span.error(flex ng-show="login.password.$touched && login.password.$error.required") Please provide your password


        md-input-container(flex)
          md-icon.fa.fa-key
            input(ng-model="user.confirmPassword" type="password" placeholder="confirm password" name="confirmPassword" ng-required="true")
            span.error(flex ng-show="login.confirmPassword.$touched && (login.confirmPassword.$modelValue !== login.Pleaseleaseassword.$modelValue)") Password doesn't match


        div(layout="row" flex layout-align="center center")
          p.md-padding.link {{status}}

        div.md-margin(layout-padding flex)
          md-button.md-raised.md-accent.md-hue-2(type="submit" ng-disabled="login.$invalid || (login.confirmPassword.$modelValue !== login.password.$modelValue)") Login
          h4
            span Create new account? &nbsp;
            a(ui-sref="sign-up") Sign-Up - It's free
